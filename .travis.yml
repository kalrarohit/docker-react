language: generic
sudo: required

services:
    - docker

before_install:
    - docker build -t rohitkalra/docker-react -f Dockerfile.dev .

script:
    # - docker run rohitkalra/docker-react npm run test -- --coverage
    - docker run -e CI=true rohitkalra/docker-react npm run test